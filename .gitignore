node_modules
dist

# AI tooling / working notes — kept locally, synced via OneDrive
CLAUDE.md
.claude/
.rtk/
docs/superpowers/
.superpowers/

# design-sync (claude.ai/design) tooling — local only, not for this public repo
.design-sync/
.ds-sync/
ds-bundle/
